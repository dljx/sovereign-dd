# Pinned 2026-07-04 to the locally tested versions — CI installs fresh on every
# cron, so a floating `>=` ships untested upstream releases straight to prod.
# Bump deliberately: update a pin, run the full test suite, then commit.
google-genai==1.68.0
requests==2.33.0
yfinance==1.2.0
pandas==2.3.3
python-dotenv==1.2.2
rich==14.3.3
finvizfinance==1.3.0
beautifulsoup4==4.14.3   # already a finvizfinance transitive dep; pinned explicitly now that dossier.py imports it directly (Finviz peg_lt fetch)
tigeropen==3.6.0
